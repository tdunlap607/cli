name: Lint, Test, Build

on:
  push:
    tags-ignore:
    - 'v*'
    branches:
    - "**"
  pull_request:

jobs:
  lintTestBuild:
    name: Lint, Test, Build
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        go: [ '1.17', '1.18' ]
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Setup Go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}
      -
        name: golangci-lint
        uses: golangci/golangci-lint-action@v2
        with:
          version: 'v1.45.2'
          args: --config <(curl https://gist.githubusercontent.com/dopey/291bd48fb973be48fe9db94a19a42442/raw/8ffb0ea6d115ff8fb28500aacdcaf021f384d44c/golangci.yml) --timeout=30m
      -
        name: Test, Build
        id: lintTestBuild
        run: V=1 make ci
      -
        name: Codecov
        uses: codecov/codecov-action@v1.2.1
        if: matrix.go == '1.18'
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.out # optional
          name: codecov-umbrella # optional
          fail_ci_if_error: true # optional (default = false)
